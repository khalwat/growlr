{% extends "layouts/growlr-base.twig" %}

{# -- Content that appears before the primary content block -- #}
{% block preContent %}
    {{ parent() }}
    <section>
    </section>
{% endblock %}

    {# -- Primary content block -- #}
    {% block content %}
        <section>
        </section>
    {% endblock %}

    {# -- Content that appears after the primary content block -- #}
    {% block postContent %}
        <section>
            <div class="h-auto w-full pt-20">
                <img class="object-cover h-auto w-full" src="/assets/puppy-pawtner.jpg"/>
            </div>
        </section>
    {% endblock %}

    {# -- Any <script></script> tags that should be included before </body> -- #}
    {% block bodyScripts %}
        {{ parent() }}
    {% endblock bodyScripts %}
